export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      {/* Add additional elements for the about layout here */}
      {children}
    </div>
  );
}
